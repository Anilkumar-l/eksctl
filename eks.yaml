# spot-cluster.yaml

apiVersion:
kind: ClusterConfig

metadata:
  name: roboshop
  region: us-east-1

managedNodeGroups:
-  name: spot
   instancetypes: ["c3.large","c4.large","c5.large"]
   spot: true
   desiredCapacity: 2
